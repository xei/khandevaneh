<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.tvnasim.khandevaneh">

    <application
        android:name=".app.App"
        android:allowBackup="false"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/KhandevanehTheme">
        <activity android:name=".app.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".app.UpdateActivity" />
        <activity android:name=".account.ProfileActivity" />
        <activity android:name=".leaderboard.LeaderBoardActivity" />
        <activity android:name=".account.LoginActivity"></activity>
    </application>

</manifest>